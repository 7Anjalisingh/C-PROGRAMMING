#include<stdio.h>
int main(0
{
int i,j,rows;
int count=1;
 printf("enter the no. of rows");
 scanf("%d",&rows);
  for(i=0;i<rows;i++)
  {
  for(j=0;j<=i;j++)
  {
  printf("%d", count);
  count=!count;
  }
  count=i%2;
  printf("\n"0;
 }
return (0);
